<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ═══════════ الأساسية ═══════════ -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ═══════════ المرحلة 3: الجدولة والعمل الخلفي ═══════════ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ═══════════ المرحلة 5: الصوت ═══════════ -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- ═══════════ النافذة العائمة (SYSTEM_ALERT_WINDOW) ═══════════ -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name="${applicationName}"
        android:label="وكيل الأتمتة"
        android:icon="@mipmap/ic_launcher">

        <!-- النشاط الرئيسي -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ═══ المرحلة 1: خدمة الإمكانية (التحكم بواجهات التطبيقات) ═══ -->
        <service
            android:name=".automation.AgentAccessibilityService"
            android:exported="true"
            android:label="محرك الأتمتة"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ═══ المرحلة 3 + 5: الخدمة الأمامية (specialUse + microphone) ═══ -->
        <service
            android:name=".automation.AgentForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse|microphone"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Voice assistant automation" />
        </service>

        <!-- ═══ المرحلة 3: مستقبل التنبيهات الدقيقة ═══ -->
        <receiver
            android:name=".automation.TaskAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- ═══ Shizuku Provider — ضروري لاستلام الـ Binder على أندرويد 11+ ═══ -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            tools:replace="android:authorities" />

        <!-- ═══ المساعد الصوتي الرسمي (VoiceInteractionService) ═══ -->
        <service
            android:name=".automation.AgentVoiceInteractionService"
            android:exported="true"
            android:label="وكيل الأتمتة"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
        </service>

        <service
            android:name=".automation.AgentVoiceSessionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />

        <service
            android:name=".automation.AgentRecognitionService"
            android:exported="true"
            android:label="وكيل الأتمتة"
            android:permission="android.permission.BIND_RECOGNITION_SERVICE">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!--
      Package Visibility (أندرويد 11 / API 30+):
      بدون الإعلان عن حزمة Shizuku يتعذّر اكتشاف الخدمة وطلب الصلاحية
      حتى لو كان تطبيق Shizuku يعمل — pingBinder يرجع false بصمت.
    -->
    <queries>
        <package android:name="moe.shizuku.privileged.api" />
        <package android:name="moe.shizuku.manager" />
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <intent>
            <action android:name="android.service.voice.VoiceInteractionService" />
        </intent>
    </queries>
</manifest>
